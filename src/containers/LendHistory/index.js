import React, { Component } from 'react';
import LendHistory from '../../components/LendHistory';

export default class LendHistoryContainer extends Component {

    render(){
        return(
            <LendHistory />
        )
    }
}