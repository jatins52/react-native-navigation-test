import React, { Component } from 'react';
import Borrow from '../../components/Borrow';

export default class BorrowContainer extends Component {

    render(){
        return(
            <Borrow />
        )
    }
}