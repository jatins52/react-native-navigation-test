import React, { Component } from 'react';
import BorrowRequestTime from '../../components/BorrowRequestTime';

export default class BorrowRequestTimeContainer extends Component {

    render(){
        return(
            <BorrowRequestTime />
        )
    }
}