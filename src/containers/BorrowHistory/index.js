import React, { Component } from 'react';
import BorrowHistory from '../../components/BorrowHistory';

export default class BorrowHistoryContainer extends Component {

    render(){
        return(
            <BorrowHistory />
        )
    }
}